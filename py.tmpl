# Code generated by curl2py. DO NOT EDIT.
# Code generated from {{.InputFile}}
import subprocess
import json
{{range .Commands}}
def {{.Name}}({{join .Params ", "}}):
    cmd = {{.Curl}}
    {{- range .Params}}
    cmd = cmd.replace("{{"{{"}}{{.}}{{"}}"}}", str({{.}}))
    {{- end}}
    resp = subprocess.run(cmd, shell=True, capture_output=True, text=True)
    return json.loads(resp.stdout)
{{- end}}
