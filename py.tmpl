#!/usr/bin/env python3

# Code generated by curl2py (https://github.com/TrucJ/curl2py). DO NOT EDIT.
# Code generated from {{.InputFile}} on {{.Date}}.

import subprocess
import json
{{range .Commands}}
def {{.Name}}({{join .Params ", "}}):
    cmd = {{.Curl}}
    {{- range .Params}}
    cmd = cmd.replace("{{"{{"}}{{.}}{{"}}"}}", str({{.}}))
    {{- end}}
    resp = subprocess.run(cmd, shell=True, capture_output=True, text=True)
    return json.loads(resp.stdout)
{{- end}}
